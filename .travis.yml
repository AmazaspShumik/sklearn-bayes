language: python
python:
  - "2.7"
virtualenv:
  system_site_packages: true
install:
  - sudo pip install numpy scipy sklearn
  - sudo pip install -r requirements.txt
  - "sudo rm -rf /dev/shm && sudo ln -s /run/shm /dev/shm"
# command to run tests
script: nosetests
